<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
        <!-- Customize your light theme here. -->
    <style name="Base.Theme.ECommerce" parent="Theme.AppCompat.NoActionBar">
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ECommerce" parent="Base.Theme.ECommerce" />

    <style name="roundedProduct" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">6dp</item>
        <item name="color">@color/white</item>
    </style>

    <style name="topRoundedCorner" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">6dp</item>
        <item name="cornerSizeTopRight">6dp</item>
        <item name="color">@color/white</item>
    </style>

    <style name="BottomSheetDialogStyle" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/bottomSheetBackground</item>
    </style>

    <style name="bottomSheetBackground" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/top_rounded_corner</item>
    </style>
</resources>